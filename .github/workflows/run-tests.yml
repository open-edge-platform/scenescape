name: Run Dummy Test

on:
    push:
        branches: [ main ]
    pull_request:

jobs:
    dummy-test:
        runs-on: ubuntu-latest
        steps:
          - name: Checkout code
            uses: actions/checkout@v4

          - name: Run dummy test
            run: |
              echo "Running dummy test..."
              echo "Dummy test passed!"