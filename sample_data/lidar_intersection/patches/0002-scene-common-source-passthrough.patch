diff --git a/scene_common/src/scene_common/detections_builder.py b/scene_common/src/scene_common/detections_builder.py
index 94478e28..11bb8663 100644
--- a/scene_common/src/scene_common/detections_builder.py
+++ b/scene_common/src/scene_common/detections_builder.py
@@ -263,6 +263,8 @@ def prepareObjDict(scene, obj, update_visibility, include_sensors=False,
 
   if hasattr(aobj, 'confidence'):
     obj_dict['confidence'] = aobj.confidence
+  if getattr(aobj, 'source', None):
+    obj_dict['source'] = aobj.source
   if hasattr(aobj, 'similarity'):
     obj_dict['similarity'] = aobj.similarity
   if hasattr(aobj, 'first_seen'):
diff --git a/scene_common/src/scene_common/ingestion.py b/scene_common/src/scene_common/ingestion.py
index 6504d362..73f12e55 100644
--- a/scene_common/src/scene_common/ingestion.py
+++ b/scene_common/src/scene_common/ingestion.py
@@ -166,6 +166,9 @@ class SceneDataIngestion:
     # but Analytics _updateVisible also treats empty as fill-eligible.
     obj.visibility = obj_data.get('visibility')
     obj.info = {'category': obj.category, 'confidence': obj.confidence}
+    # Debug aid carried through from the publisher (e.g. "lidar"/"camera");
+    # not part of the curated obj.info reconstruction above.
+    obj.source = obj_data.get('source')
 
     # Reconstruct bbMeters from size when available
     if obj.size and len(obj.size) == 3:
