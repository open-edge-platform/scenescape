CPU,CPU:  decodebin force-sw-decoders=true ! video/x-raw         ! gvapython ... ! gvadetect model=... device=CPU                                        ! queue ! gvametaconvert ...                                                                                ! gvapython class=PostInferenceDataPublish ... ! appsink ...
AUTO,CPU: decodebin3 ! video/x-raw                               ! gvapython ... ! gvadetect model=... device=CPU                                        ! queue ! gvametaconvert ...                                                                                ! gvapython class=PostInferenceDataPublish ... ! appsink ...
AUTO,GPU: decodebin3 ! video/x-raw(memory:VAMemory)              ! gvapython ... ! gvadetect model=... device=GPU pre-process-backend=va-surface-sharing ! queue ! gvametaconvert ... ! vapostproc ! video/x-raw,format=BGRA ! videoconvert ! video/x-raw,format=BGR ! gvapython class=PostInferenceDataPublish ... ! appsink ...
CPU,GPU:  decodebin force-sw-decoders=true ! video/x-raw         ! gvapython ... ! gvadetect model=... device=GPU pre-process-backend=opencv             ! queue ! gvametaconvert ... ! vapostproc ! video/x-raw,format=BGRA ! videoconvert ! video/x-raw,format=BGR ! gvapython class=PostInferenceDataPublish ... ! appsink ...
GPU,CPU:  decodebin3 ! vapostproc ! video/x-raw                  ! gvapython ... ! gvadetect model=... device=CPU                                        ! queue ! gvametaconvert ...                                                                                ! gvapython class=PostInferenceDataPublish ... ! appsink ...
GPU,GPU:  decodebin3 ! vapostproc ! video/x-raw(memory:VAMemory) ! gvapython ... ! gvadetect model=... device=GPU pre-process-backend=va-surface-sharing ! queue ! gvametaconvert ... ! vapostproc ! video/x-raw,format=BGRA ! videoconvert ! video/x-raw,format=BGR ! gvapython class=PostInferenceDataPublish ... ! appsink ...
